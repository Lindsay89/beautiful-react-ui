export type BaseProps = {
  /**
   * the id to be applied to the component's element
   */
  id?: string,
  /**
   * the CSS class name to be merged within the component's classes
   */
  className?: string,
  /**
   * the CSS style rules to be applied to the component's element
   */
  style?: any,
};
