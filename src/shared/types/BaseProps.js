import PropTypes from 'prop-types';

export default {
  /**
   * the id to be applied to the component's element
   */
  id: PropTypes.string,
  /**
   * the CSS classes to be merged within the component's classes
   */
  className: PropTypes.string,
  /**
   * the CSS rules to be applied to the component's element
   */
  style: PropTypes.object,
};
