import useWindowScroll from './useWindowScroll';

// TODO: test this hook
describe('useWindowScroll hook', () => {
  it('should be a function', () => {
    expect(useWindowScroll).to.be.a('function');
  });
});
